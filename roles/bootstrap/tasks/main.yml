---
- import_tasks: "{{role_path}}/tasks/update.yml"
  tags: bootstrap

- import_tasks: "{{role_path}}/tasks/users.yml"
  tags: bootstrap

- import_tasks: "{{role_path}}/tasks/ssh.yml"
  tags: bootstrap

- import_tasks: "{{role_path}}/tasks/docker.yml"
  tags: bootstrap

- import_tasks: "{{role_path}}/tasks/swarm-init.yml"
  tags: bootstrap

- import_tasks: "{{role_path}}/tasks/swarm-manager.yml"
  tags: bootstrap

- import_tasks: "{{role_path}}/tasks/swarm-worker.yml"
  tags: bootstrap

- import_tasks: "{{role_path}}/tasks/ansible.yml"
  tags: bootstrap